#import gui.py


def recommend_movies(movie_name):
    # Use the movie's synopsis to make recommendations
    # Preprocess the synopsis data
    # Use spaCy for tokenization and part-of-speech tagging
    # Convert the preprocessed data into TF-IDF vectors
    # Calculate the cosine similarity between the TF-IDF vectors to find similar movies
    # Display the recommended movies in the GUI
    pass